# Local environment and secrets
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
*.token
*.tok
*.secret
*.secrets
credentials.json
auth.json
secrets/

# Local AI / runtime artifacts
models/
*.gguf
*.safetensors
*.bin
*.onnx
*.mlmodel
*.pt
*.pth
*.ckpt
*.rdb
*.sqlite
*.db
*.sqlite3
dump.rdb
*.mp3
*.wav
*.m4a
*.epub
# Negation: the ebook-converter checks in a deterministic fixture EPUB under
# app/ebook-converter/samples/ as a stable target for image-preservation
# regression tests (Phase 2 of NEXT_UP_PLAN.md). The blanket *.epub rule
# above would otherwise ignore it.
!app/ebook-converter/samples/**/*.epub
!app/ebook-converter/samples/**/*.EPUB
# Phase 4.1: a SECOND, much smaller fixture EPUB under
# app/ebook-converter/e2e/fixtures/ serves as the Playwright E2E
# target. Keep it tracked for the same reason as the Phase 2 sample
# (deterministic byte-stable test fixture).
!app/ebook-converter/e2e/fixtures/**/*.epub
!app/ebook-converter/e2e/fixtures/**/*.EPUB

# Local backups and sample content
backups/
exmple-books/

# Local tool state and secrets (kept outside the repo on purpose)
.claude/
hg_token.md
omlx-home/
opencode-home/
opencode-runtime/
# VieNeu-TTS is a plain local dir (cloned by scripts/setup_vieneu.sh) —
# the venv (~1.2 GB model cache on first import) stays out of the repo.
app/tts-service/VieNeu-TTS/.venv/
app/tts-service/VieNeu-TTS/models/
app/tts-service/VieNeu-TTS/voices/
# The cloned source itself is also gitignored — re-run setup_vieneu.sh to
# restore it. This keeps the repo small and avoids touching upstream.
app/tts-service/VieNeu-TTS/

# F5-TTS is a plain local dir (not a submodule): venv, ~1.3GB weights and the
# prepared reference clips all stay out of the repo. `vocab.txt` is the one
# exception — it ships with the checkpoint and cannot be regenerated.
app/tts-service/F5-TTS/.venv/
app/tts-service/F5-TTS/models/
app/tts-service/F5-TTS/voices/
!app/tts-service/F5-TTS/models/vietnamese/vocab.txt

# Python / Node / app caches
.venv/
.venv*/
venv/
venv*/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
pip-wheel-metadata/
node_modules/
.next/
out/
dist/
build/
coverage/

# Logs and local data
*.log
logs/
tmp/
data/
cache/
playwright-report/
test-results/
# Phase 4.1: .seed-book.json is written by
# `app/ebook-converter/e2e/seed-fixture.global-setup.ts` once per
# Playwright run; the resolved book ID shifts each run so the file
# must not be checked in.
app/ebook-converter/e2e/.seed-book.json

# OS/editor noise
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Local backup / temporary files
*.bak
*.orig
*.rej
*.ipynb_checkpoints/

# TypeScript incremental build cache and ad-hoc scratch files
# (the latter matches _backfill_*.js and _check_*.js in app/ebook-converter/ —
#  promote them to scripts/ if you want them tracked)
*.tsbuildinfo
_*.[jt]s
_*.[jt]sx
_*.py

# Reference material checked out locally for offline docs (submodule candidates)
reference
reference/

# VS Code workspace files (personal IDE config, not for sharing)
*.code-workspace

hg_token.md

