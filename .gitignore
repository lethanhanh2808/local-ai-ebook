# Local environment and secrets
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
*.token
*.tok
*.secret
*.secrets
credentials.json
auth.json
secrets/

# Local AI / runtime artifacts
models/
*.gguf
*.safetensors
*.bin
*.onnx
*.mlmodel
*.pt
*.pth
*.ckpt
*.rdb
*.sqlite
*.db
*.sqlite3
dump.rdb
*.mp3
*.wav
*.m4a
*.epub

# Local backups and sample content
backups/
exmple-books/

# Local tool state and secrets (kept outside the repo on purpose)
.claude/
hg_token.md
omlx-home/
opencode-home/
opencode-runtime/
# VieNeu-TTS is intentionally vendored as a git submodule (see .gitmodules);
# its working tree + .venv stay on disk but are tracked via the submodule pointer.

# Python / Node / app caches
.venv/
.venv*/
venv/
venv*/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
pip-wheel-metadata/
node_modules/
.next/
out/
dist/
build/
coverage/

# Logs and local data
*.log
logs/
tmp/
data/
cache/
playwright-report/
test-results/

# OS/editor noise
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Local backup / temporary files
*.bak
*.orig
*.rej
*.ipynb_checkpoints/

# TypeScript incremental build cache and ad-hoc scratch files
# (the latter matches _backfill_*.js and _check_*.js in app/ebook-converter/ —
#  promote them to scripts/ if you want them tracked)
*.tsbuildinfo
_*.[jt]s
_*.[jt]sx
_*.py

# Reference material checked out locally for offline docs (submodule candidates)
reference/
